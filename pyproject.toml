[tool.poetry]
name = "graphlib"
version = "1.0.0"
description = "Backport of the Python 3.9 graphlib module for Python 3.8+"
authors = ["Marius Helf <helfsmarius@gmail.com>"]
license = "PSF"

[tool.poetry.dependencies]
python = "^3.7"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry>=1.0"]
build-backend = "poetry.masonry.api"
